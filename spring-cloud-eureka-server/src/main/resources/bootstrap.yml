# YAML configuration file for Spring Cloud Eureka

# Log level management for packages
# logging.level.com.netflix: DEBUG
# logging.level.org.springframework: DEBUG

spring:
  application:
    name: eureka-server

cloud:
    config:
      uri: http://localhost:8888    # Config server location